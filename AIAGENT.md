# AI Agent Working Rules

이 저장소에서 작업하는 AI 에이전트는 아래 규칙을 따른다.

## Allowed Without Approval

- 파일 확인, 검색, 읽기, 디렉터리 탐색은 사용자 허락 없이 수행할 수 있다.
- 현재 상태 파악을 위한 `git status`, `rg`, `find`, `ls` 같은 조회성 명령은 자유롭게 수행할 수 있다.

## Explicit Approval Required Before Any File Change

- 명시적 승인 없이는 어떤 파일도 새로 만들거나 수정하거나 삭제할 수 없다.
- 파일 변경이 필요하면 먼저 사용자에게 어떤 파일을 왜 바꾸는지 짧고 명확하게 설명해야 한다.
- 사용자가 분명하게 승인한 뒤에만 파일 생성, 파일 수정, 파일 삭제를 진행할 수 있다.
- 기존 파일 덮어쓰기, 파일명 변경, 내용 일부만 바꾸는 작업도 모두 파일 변경으로 간주한다.

## Expected Communication

- 파일 변경이 필요하면 어떤 파일을 왜 바꾸는지 먼저 알린다.
- 사용자가 명시적으로 승인하지 않았다면 어떤 파일 변경도 진행하지 않는다.
- 탐색 단계와 변경 단계를 구분해서 안내한다.
